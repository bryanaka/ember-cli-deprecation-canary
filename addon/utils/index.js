import assertEmberDebug from './assert-ember-debug';
import assertHasDeprecationWorkflow from './assert-has-deprecation-workflow';

export {
  assertEmberDebug,
  assertHasDeprecationWorkflow,
};

export default {
  assertEmberDebug,
  assertHasDeprecationWorkflow,
};
